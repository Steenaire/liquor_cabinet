<% @recipes.each do |recipe| %>
            <div class="col-xs-12">
              <div class="item-block">
                <header>
                  <% if recipe.card_image %>
                    <img src="img/Bayleaf.jpg" alt="">
                  <% else %>
                    <i class="fa fa-glass" aria-hidden="true"></i>
                  <% end %>

                  <div class="hgroup">
                    <h4><a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a></h4>
                    <% if recipe.average_rating > 3 %>
                      <h5><span class="label label-warning">Popular</span></h5>
                    <% end %>
                  </div>
                  <div class="header-meta">
                    <span><a href="/users/<%= recipe.user.id %>">Submitted by <%= recipe.user.username %></a></span>
                  </div>
                </header>
                <div class="item-body">
                  <p><%= recipe.description %></p>
                </div>

                <footer>
                  <span>
                  <!-- This is all for handling ratings -->
                    <% if current_user&&current_user.ratings.find_by(recipe_id: recipe.id) %>
                          <% if current_user.ratings.find_by(recipe_id: recipe.id).score == 5 %>
                            <%= render partial: "rated_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @gold_star, color4: @gold_star, color5: @gold_star} %>
                          <% elsif current_user.ratings.find_by(recipe_id: recipe.id).score == 4 %>
                            <%= render partial: "rated_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @gold_star, color4: @gold_star, color5: @grey_star} %>
                          <% elsif current_user.ratings.find_by(recipe_id: recipe.id).score == 3 %>
                            <%= render partial: "rated_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @gold_star, color4: @grey_star, color5: @grey_star} %>
                          <% elsif current_user.ratings.find_by(recipe_id: recipe.id).score == 2 %>
                            <%= render partial: "rated_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @grey_star, color4: @grey_star, color5: @grey_star} %>
                          <% elsif current_user.ratings.find_by(recipe_id: recipe.id).score == 1 %>
                            <%= render partial: "rated_stars", locals: {color1: @gold_star, color2: @grey_star, color3: @grey_star, color4: @grey_star, color5: @grey_star} %>
                          <% end %>
                    <% elsif current_user %>
                      <% if recipe.average_rating.round == 5 %>
                        <%= render partial: "unrated_user_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @gold_star, color4: @gold_star, color5: @gold_star, current_user: current_user, recipe: recipe} %>
                      <% elsif recipe.average_rating.round == 4 %>
                        <%= render partial: "unrated_user_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @gold_star, color4: @gold_star, color5: @grey_star, current_user: current_user, recipe: recipe} %>
                      <% elsif recipe.average_rating.round == 3 %>
                        <%= render partial: "unrated_user_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @gold_star, color4: @grey_star, color5: @grey_star, current_user: current_user, recipe: recipe} %>
                      <% elsif recipe.average_rating.round == 2 %>
                        <%= render partial: "unrated_user_stars", locals: {color1: @gold_star, color2: @gold_star, color3: @grey_star, color4: @grey_star, color5: @grey_star, current_user: current_user, recipe: recipe} %>
                      <% elsif recipe.average_rating.round == 1 %>
                        <%= render partial: "unrated_user_stars", locals: {color1: @gold_star, color2: @grey_star, color3: @grey_star, color4: @grey_star, color5: @grey_star, current_user: current_user, recipe: recipe} %>
                      <% else %>
                        <%= render partial: "unrated_user_stars", locals: {color1: @grey_star, color2: @grey_star, color3: @grey_star, color4: @grey_star, color5: @grey_star, current_user: current_user, recipe: recipe} %>
                      <% end %>
                    <% else %>
                      <% if recipe.average_rating == 5 %>
                        <%= render partial: "rated_stars", locals: {color1: @red_star, color2: @red_star, color3: @red_star, color4: @red_star, color5: @red_star} %>
                      <% elsif recipe.average_rating == 4 %>
                        <%= render partial: "rated_stars", locals: {color1: @red_star, color2: @red_star, color3: @red_star, color4: @red_star, color5: @grey_star} %>
                      <% elsif recipe.average_rating == 3 %>
                        <%= render partial: "rated_stars", locals: {color1: @red_star, color2: @red_star, color3: @red_star, color4: @grey_star, color5: @grey_star} %>
                      <% elsif recipe.average_rating == 2 %>
                        <%= render partial: "rated_stars", locals: {color1: @red_star, color2: @red_star, color3: @grey_star, color4: @grey_star, color5: @grey_star} %>
                      <% elsif recipe.average_rating == 1 %>
                        <%= render partial: "rated_stars", locals: {color1: @red_star, color2: @grey_star, color3: @grey_star, color4: @grey_star, color5: @grey_star} %>
                      <% else %>
                        <%= render partial: "rated_stars", locals: {color1: @grey_star, color2: @grey_star, color3: @grey_star, color4: @grey_star, color5: @grey_star} %>
                      <% end %>
                    <% end %>

                    
                    <span class="review">
                      (<span><%= recipe.total_ratings %></span>)
                    </span>
                  </span>
                </p>
                  <p class="status"><strong></strong></p>
                  <p class="status"><strong></strong></p>

                  <div class="action-btn">
                    <a class="btn btn-xs btn-danger" href="/recipes/<%= recipe.id %>">Make This!</a>
                  </div>
                </footer>
              </div>
            </div>
          <% end %>
          <!-- END Job item -->
          <!-- END Restaurant detail -->