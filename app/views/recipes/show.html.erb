<!-- Content Start -->



<!-- Start Pages Title  -->

<section id="page-title" class="page-title-style2">
  <div class="color-overlay"></div>
  <div class="container inner-img">
    <div class="row">
      <div class="Page-title">
        <div class="col-md-12 text-center">
          <div class="title-text">
            <h6>Great Service</h6>
            <h2 class="page-title"><%= @recipe.name %></h2>
          </div>
        </div>
        <div class="col-md-12 text-center">
          <div class="breadcrumb-trail breadcrumbs">
            <span class="trail-begin"><a href="#">Home</a></span>
            <span class="sep">/</span> <span class="trail-end">Faq</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- End Pages Title  -->

<!-- Main container -->
<main class="main-content">
  <!-- Start Faq Page -->

  <section id="faq-result">
    <div class="faq-all-content">
      <section class="light-wrapper">

        <div class="container">
                    <div class="inner">
                        <header class="section-header text-left">
                            <h2><%= @recipe.name %></h2>
                            <span><%= @recipe.description %></span>
                        </header>

                        <ul class="faq-items">
                            <li>
                                <h5>Ingredients:</h5>
                                  <% @recipe.recipe_ingredients.each do |recipe_ingredient| %>
                                    <p><%= recipe_ingredient.measure_unit(@unit) %>
                                    <%= recipe_ingredient.brand.name %>
                                    <%= recipe_ingredient.ingredient.name %></p>
                                  <% end %>
                            </li>

                            <li>
                                <h5>Directions:</h5>
                                <p><%= @recipe.instructions %></p>
                            </li>

                            <li>
                                <h5>Convert to:</h5>
                                <a href="/recipes/<%= @recipe.id %>?<%= @convert_to %>=true"><p><%= @convert_to %></p></a>
                            </li>


                        </ul>
                        <div class="action-btn">
                          <%= form_tag '/timeline_drinks', method: :post do %>
                            <%= label_tag :comment %>
                            <%= text_field_tag :comment %>
                            <%= hidden_field_tag :user_id, current_user.id %>
                            <%= hidden_field_tag :recipe_id, @recipe.id %>
                            <%= submit_tag "I Made This!", class: "btn btn-xs btn-danger" %>
                          <% end %>

                          
                        </div>

                        <div ng-app="app">
                          <div ng-controller="recipesCtrl" ng-init="setup()">

                            <div ng-click="recipe.visibility = !recipe.visibility">
                              <a>Click to show Comments (<%= @recipe.return_comments.length %>)</a>
                              <div ng-show="recipe.visibility">
                                
                                <% @recipe.return_comments.each do |drink| %>

                                    <div class="col-xs-9">
                                      <div class="item-block">
                                        <header>
        
                                          <div class="header-meta">
                                            <span><a href="/users/<%= drink.recipe.user.id %>">Submitted by <%= drink.recipe.user.username %></a></span>
                                          </div>
                                        </header>
                                        <div class="item-body">
                                          <p>Made on <%= human_time(drink.created_at.in_time_zone(drink.recipe.user.timezone)) %></p>
                                          <% if drink.comment %>
                                            <p><%= drink.recipe.user.username %> Says: <span class="comment">"<%= drink.comment %>"</span></p>
                                          <% end %>
                                        </div>

                                      <footer>
                                        <span>

                                        </span>
                                      </p>
                                        <p class="status"><strong></strong></p>
                                        <p class="status"><strong></strong></p>

                                      </footer>
                                    </div>
                                  </div>

                                <% end %>


                              </div>
                            </div>

                          </div>
                        </div>

                        </div>

                      </div>
                

        </div>
      </section>


    </div>
  </section>

  <!-- End Faq Page -->

</main>
<!-- END Main container -->



<!-- Content End -->